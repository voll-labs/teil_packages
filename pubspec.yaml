name: teil_packages_workspace
publish_to: none
repository: https://github.com/voll-labs/teil_packages

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  melos: ^5.2.2
  teil_lints:
    path: packages/teil_lints
